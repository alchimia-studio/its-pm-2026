# Duello tra Cannoni v1.1 — Vento, Power-Up, Biomi — Piano di implementazione

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aggiungere al gioco esistente (v1) tre funzionalità indipendenti — vento, bersaglio power-up con modalità power, biomi estetici del terreno — come da spec approvata.

**Architecture:** Si estendono i moduli esistenti (`types`, `physics`, `engine`, `render`), senza nuovi file: ogni funzionalità aggiunge campi allo stato di gioco e viene letta/scritta dai moduli che già se ne occupano (fisica → `physics.ts`, regole/stato → `engine.ts`, disegno → `render.ts`).

**Tech Stack:** Next.js 16, TypeScript, Canvas 2D. Nessuna libreria esterna.

## Global Constraints

- Vita 100%, −20% per colpo diretto normale; **−40% (doppio) se il tiratore è in modalità power**.
- Terreno fisso, simmetrico rispetto al centro, con piattaforme sotto i cannoni: **invariato** da questo lavoro.
- **Vento**: un'accelerazione orizzontale costante sulla palla in volo; direzione e intensità casuali, **fisse per un intero round (i due tiri di entrambi i giocatori)**, poi ri-sorteggiate; intensità massima **leggera-moderata**; indicatore **sempre visibile** in HUD (freccia + valore).
- **Bersaglio power-up**: compare a inizio turno con probabilità ~20-25% **solo se** non c'è già un bersaglio attivo e **nessun cannone è in modalità power**; resta visibile per **due tiri consecutivi**; si colpisce con **il tiro normale del turno** (non un'azione separata); ordine dei controlli in volo: **bersaglio → uscita dai lati → cannone nemico → terreno**.
- **Modalità power**: attivata colpendo il bersaglio, dura **3 turni propri successivi** del giocatore che l'ha attivata (il tiro che ha colpito il bersaglio **non** è uno dei 3); il cannone si disegna **il doppio più grande** con **aura luminosa**; la sua **sagoma-bersaglio raddoppia** (più facile da colpire per l'avversario mentre è powered); ogni suo colpo a segno in questi turni fa **doppio danno**; subire un colpo mentre si è powered **non cambia** (sempre −20% normale).
- **Biomi**: uno scelto **a caso una sola volta a inizio partita**, applicato a **tutto** il terreno; cambia **solo la tavolozza colori del terreno** (riempimento + bordo); quattro biomi: **Erba** (attuale), **Sabbia**, **Neve**, **Spiaggia**. Cielo, stelle, cannoni, palla e HUD restano invariati in ogni bioma.
- **Niente test automatici** (policy di progetto — vedi `CLAUDE.md`): la verifica di ogni task è `npm run lint` + `npm run build` (dove pertinente) + **playtest manuale** nel browser.
- Messaggi di commit in italiano.

---

## Struttura dei file

| File | Estensione | Task |
|---|---|---|
| `src/game/types.ts` | Tipi `Wind`, `Biome`, `PowerTarget`, `PowerTurns`; costanti `TARGET_RADIUS`, `POWER_SIZE_MULTIPLIER`; nuovi campi in `GameState` | 1, 2, 3 |
| `src/game/physics.ts` | Costante `WIND_MAX_ACCEL`, funzione `windAccelFor()`, `stepBall()` accetta l'accelerazione del vento | 1 |
| `src/game/engine.ts` | Generazione/ri-sorteggio del vento; scelta del bioma; comparsa/scadenza del bersaglio; attivazione/decremento della modalità power; danno raddoppiato; sagoma raddoppiata | 1, 2, 3 |
| `src/game/render.ts` | Indicatore vento in HUD; tavolozze colore per bioma; disegno del bersaglio; cannone ingrandito con aura; badge turni di power in HUD | 1, 2, 3 |

Nessun nuovo file. Ordine di implementazione: **Vento → Biomi → Bersaglio power-up**, dal più semplice al più articolato; ogni task lascia il gioco pienamente giocabile e verificabile.

---

## Task 1: Vento

**Files:**
- Modify: `src/game/types.ts`
- Modify: `src/game/physics.ts`
- Modify: `src/game/engine.ts`
- Modify: `src/game/render.ts`

**Interfaces:**
- Produce: `Wind` (`{ dirX: -1 | 1; strength: number }`), `windAccelFor(wind: Wind): number`, campi `wind: Wind` e `shotsSinceWindChange: number` in `GameState`.
- Consuma: nessuna dipendenza da altri task.

- [ ] **Step 1: Aggiungi il tipo `Wind` e i nuovi campi a `GameState`**

In `src/game/types.ts`, aggiungi (dopo l'interfaccia `AimState` e prima di `GameState`):

```ts
/** Il vento: spinge la palla in orizzontale mentre è in volo. */
export interface Wind {
  dirX: -1 | 1; // verso: -1 sinistra, 1 destra
  strength: number; // 0..1, quanto è forte
}
```

Poi modifica `GameState` aggiungendo due campi (dopo `maxDrag`):

```ts
export interface GameState {
  width: number;
  height: number;
  maxDrag: number; // lunghezza di trascinamento che dà la potenza massima
  wind: Wind;
  shotsSinceWindChange: number; // tiri conclusi col vento attuale (0 o 1); a 2 si ri-sorteggia
  phase: Phase;
  current: PlayerId; // di chi è il turno
  winner: PlayerId | null;
  terrain: Terrain;
  cannons: Record<PlayerId, Cannon>;
  ball: Ball;
  aim: AimState;
}
```

- [ ] **Step 2: Aggiungi l'accelerazione del vento alla fisica**

In `src/game/physics.ts`, sostituisci l'intero contenuto con:

```ts
// src/game/physics.ts
// Moto parabolico del proiettile. Funzioni pure.
// Convenzione: y cresce verso il basso, quindi la gravità è positiva e
// per sparare verso l'alto la componente verticale della velocità è negativa.

import type { Ball, Wind } from "./types";

export const GRAVITY = 900; // px/s^2 (verso il basso)
export const MIN_SPEED = 250; // px/s (potenza 0%)
export const MAX_SPEED = 1100; // px/s (potenza 100%)
export const WIND_MAX_ACCEL = 260; // px/s^2: accelerazione orizzontale al 100% di intensità del vento

function clamp01(v: number): number {
  return Math.max(0, Math.min(1, v));
}

/** Converte potenza (0..1) e angolo (radianti) nella velocità iniziale. */
export function launchVelocity(
  power: number,
  angle: number,
): { vx: number; vy: number } {
  const speed = MIN_SPEED + clamp01(power) * (MAX_SPEED - MIN_SPEED);
  return { vx: Math.cos(angle) * speed, vy: Math.sin(angle) * speed };
}

/** Converte lo stato del vento nell'accelerazione orizzontale che applica alla palla. */
export function windAccelFor(wind: Wind): number {
  return wind.dirX * clamp01(wind.strength) * WIND_MAX_ACCEL;
}

/** Avanza la palla di un passo dt (secondi); windAccelX è l'accelerazione orizzontale del vento. */
export function stepBall(ball: Ball, dt: number, windAccelX = 0): Ball {
  const vx = ball.vx + windAccelX * dt;
  const vy = ball.vy + GRAVITY * dt;
  return {
    ...ball,
    vx,
    vy,
    x: ball.x + vx * dt,
    y: ball.y + vy * dt,
  };
}
```

- [ ] **Step 3: Genera e ri-sorteggia il vento nel motore**

In `src/game/engine.ts`, aggiorna l'import dei tipi e delle funzioni fisiche. Sostituisci:

```ts
import {
  type AimState,
  type Ball,
  type GameState,
  type PlayerId,
  BALL_RADIUS,
  CANNON_RADIUS,
} from "./types";
import { flattenPlatform, generateTerrain, heightAt } from "./terrain";
import { launchVelocity, stepBall } from "./physics";
import { canStartAim, computeAim, isShotValid, maxDragFor } from "./input";
```

con:

```ts
import {
  type AimState,
  type Ball,
  type GameState,
  type PlayerId,
  type Wind,
  BALL_RADIUS,
  CANNON_RADIUS,
} from "./types";
import { flattenPlatform, generateTerrain, heightAt } from "./terrain";
import { launchVelocity, stepBall, windAccelFor } from "./physics";
import { canStartAim, computeAim, isShotValid, maxDragFor } from "./input";
```

Poi aggiungi, subito dopo la riga `function emptyBall(): Ball { ... }`, una funzione per generare un vento casuale:

```ts
function randomWind(rng: () => number): Wind {
  return { dirX: rng() < 0.5 ? -1 : 1, strength: rng() };
}
```

Nella funzione `createGame`, aggiungi `wind` e `shotsSinceWindChange` all'oggetto restituito (dopo `maxDrag: maxDragFor(width, height),`):

```ts
    maxDrag: maxDragFor(width, height),
    wind: randomWind(rng),
    shotsSinceWindChange: 0,
```

Nella funzione `update`, passa l'accelerazione del vento a `stepBall`. Sostituisci:

```ts
export function update(state: GameState, dt: number): void {
  if (state.phase !== "flying" || !state.ball.active) return;
  state.ball = stepBall(state.ball, dt);
  resolveBall(state);
}
```

con:

```ts
export function update(state: GameState, dt: number): void {
  if (state.phase !== "flying" || !state.ball.active) return;
  state.ball = stepBall(state.ball, dt, windAccelFor(state.wind));
  resolveBall(state);
}
```

Infine, nella funzione `endShot`, aggiungi il ri-sorteggio del vento ogni 2 tiri conclusi (un round), subito prima del commento "Passa il turno". Sostituisci:

```ts
  // Passa il turno.
  state.current = other(state.current);
  state.phase = "aiming";
}
```

con:

```ts
  // Vento: cambia ogni 2 tiri conclusi (un round completo di entrambi i giocatori).
  state.shotsSinceWindChange += 1;
  if (state.shotsSinceWindChange >= 2) {
    state.wind = randomWind(Math.random);
    state.shotsSinceWindChange = 0;
  }

  // Passa il turno.
  state.current = other(state.current);
  state.phase = "aiming";
}
```

- [ ] **Step 4: Disegna l'indicatore del vento in HUD**

In `src/game/render.ts`, aggiungi una funzione `drawWind` subito prima di `function drawHud`:

```ts
function drawWind(ctx: CanvasRenderingContext2D, s: GameState): void {
  const w = s.wind;
  const angle = w.dirX > 0 ? 0 : Math.PI;
  const cx = s.width / 2;
  const y = 54;
  const len = 20 + w.strength * 50;
  const tipX = cx + Math.cos(angle) * len;

  ctx.save();
  ctx.strokeStyle = COLORS.subtitle;
  ctx.fillStyle = COLORS.subtitle;
  ctx.lineWidth = 3;
  ctx.lineCap = "round";

  ctx.beginPath();
  ctx.moveTo(cx - Math.cos(angle) * len, y);
  ctx.lineTo(tipX, y);
  ctx.stroke();

  const ah = 7;
  const left = angle + Math.PI - 0.5;
  const right = angle + Math.PI + 0.5;
  ctx.beginPath();
  ctx.moveTo(tipX, y);
  ctx.lineTo(tipX + Math.cos(left) * ah, y + Math.sin(left) * ah);
  ctx.lineTo(tipX + Math.cos(right) * ah, y + Math.sin(right) * ah);
  ctx.closePath();
  ctx.fill();
  ctx.restore();

  ctx.save();
  ctx.font = "12px system-ui, sans-serif";
  ctx.fillStyle = COLORS.subtitle;
  ctx.textAlign = "center";
  ctx.fillText(`vento ${Math.round(w.strength * 100)}%`, cx, y + 18);
  ctx.restore();
}
```

Poi chiamala da `drawHud`, mostrandola sia mentre si mira sia mentre la palla è in volo. Sostituisci:

```ts
function drawHud(ctx: CanvasRenderingContext2D, s: GameState): void {
  drawHealth(ctx, 20, 20, s.cannons.red.health, COLORS.red, "ROSSO", "left");
  drawHealth(ctx, s.width - 20, 20, s.cannons.blue.health, COLORS.blue, "BLU", "right");

  if (s.phase === "aiming") {
    const name = s.current === "red" ? "ROSSO" : "BLU";
    ctx.save();
    ctx.fillStyle = playerColor(s.current);
    ctx.font = "bold 16px system-ui, sans-serif";
    ctx.textAlign = "center";
    ctx.fillText(`Turno: ${name}`, s.width / 2, 30);
    ctx.restore();
  }
}
```

con:

```ts
function drawHud(ctx: CanvasRenderingContext2D, s: GameState): void {
  drawHealth(ctx, 20, 20, s.cannons.red.health, COLORS.red, "ROSSO", "left");
  drawHealth(ctx, s.width - 20, 20, s.cannons.blue.health, COLORS.blue, "BLU", "right");

  if (s.phase === "aiming") {
    const name = s.current === "red" ? "ROSSO" : "BLU";
    ctx.save();
    ctx.fillStyle = playerColor(s.current);
    ctx.font = "bold 16px system-ui, sans-serif";
    ctx.textAlign = "center";
    ctx.fillText(`Turno: ${name}`, s.width / 2, 30);
    ctx.restore();
  }

  if (s.phase === "aiming" || s.phase === "flying") drawWind(ctx, s);
}
```

- [ ] **Step 5: Verifica lint e build**

Run: `npm run lint`
Atteso: nessun errore.

Run: `npm run build`
Atteso: "Compiled successfully".

- [ ] **Step 6: Playtest manuale**

Run: `npm run dev`, apri `http://localhost:3000/gioco`.

Verifica:
- sotto l'indicazione del turno compare una freccia con una percentuale ("vento NN%");
- la freccia e la percentuale **restano identiche** per i tiri di entrambi i giocatori in uno stesso round, poi **cambiano** all'inizio del round successivo;
- la traiettoria della palla devia visibilmente nella direzione indicata (specialmente con vento forte), senza rendere i tiri impossibili da controllare.

- [ ] **Step 7: Commit**

```bash
git add src/game/types.ts src/game/physics.ts src/game/engine.ts src/game/render.ts
git commit -m "feat(gioco): aggiunge il vento (fisica, motore, indicatore HUD)"
```

---

## Task 2: Biomi (solo estetica)

**Files:**
- Modify: `src/game/types.ts`
- Modify: `src/game/engine.ts`
- Modify: `src/game/render.ts`

**Interfaces:**
- Produce: tipo `Biome` (`"erba" | "sabbia" | "neve" | "spiaggia"`), campo `biome: Biome` in `GameState`.
- Consuma: nessuna dipendenza dal Task 1 (modifica parti diverse di `engine.ts`/`render.ts`), ma va applicato dopo il Task 1 perché entrambi toccano `createGame`.

- [ ] **Step 1: Aggiungi il tipo `Biome` e il campo a `GameState`**

In `src/game/types.ts`, aggiungi (subito dopo l'interfaccia `Wind`):

```ts
/** I quattro biomi: cambiano solo la tavolozza colori del terreno. */
export type Biome = "erba" | "sabbia" | "neve" | "spiaggia";
```

Poi aggiungi il campo `biome` a `GameState` (dopo `shotsSinceWindChange`):

```ts
  wind: Wind;
  shotsSinceWindChange: number; // tiri conclusi col vento attuale (0 o 1); a 2 si ri-sorteggia
  biome: Biome;
```

- [ ] **Step 2: Scegli un bioma casuale a inizio partita**

In `src/game/engine.ts`, aggiorna l'import dei tipi aggiungendo `Biome`. Sostituisci:

```ts
import {
  type AimState,
  type Ball,
  type GameState,
  type PlayerId,
  type Wind,
  BALL_RADIUS,
  CANNON_RADIUS,
} from "./types";
```

con:

```ts
import {
  type AimState,
  type Ball,
  type Biome,
  type GameState,
  type PlayerId,
  type Wind,
  BALL_RADIUS,
  CANNON_RADIUS,
} from "./types";
```

Poi aggiungi, subito dopo la funzione `randomWind`, l'elenco dei biomi e la funzione per sceglierne uno a caso:

```ts
const BIOMES: Biome[] = ["erba", "sabbia", "neve", "spiaggia"];

function randomBiome(rng: () => number): Biome {
  return BIOMES[Math.floor(rng() * BIOMES.length)];
}
```

Infine, nella funzione `createGame`, aggiungi `biome` all'oggetto restituito (dopo `shotsSinceWindChange: 0,`):

```ts
    wind: randomWind(rng),
    shotsSinceWindChange: 0,
    biome: randomBiome(rng),
```

- [ ] **Step 3: Applica la tavolozza del bioma al disegno del terreno**

In `src/game/render.ts`, aggiorna l'import aggiungendo `Biome`. Sostituisci:

```ts
import { type GameState, type PlayerId, BALL_RADIUS, CANNON_RADIUS } from "./types";
```

con:

```ts
import {
  type Biome,
  type GameState,
  type PlayerId,
  BALL_RADIUS,
  CANNON_RADIUS,
} from "./types";
```

Rimuovi `terrain` e `terrainEdge` dall'oggetto `COLORS` (non servono più: li sostituisce la tavolozza per bioma). Sostituisci:

```ts
const COLORS = {
  skyTop: "#0a0e24",
  skyBottom: "#141a3a",
  star: "#ffffff",
  terrain: "#1c8f5a",
  terrainEdge: "#27e0a0",
  red: "#ff3b6b",
  blue: "#3d6bff",
  ball: "#fff2a8",
  arrow: "#ffe66d",
  text: "#ffffff",
  subtitle: "#cfd6ff",
} as const;
```

con:

```ts
const COLORS = {
  skyTop: "#0a0e24",
  skyBottom: "#141a3a",
  star: "#ffffff",
  red: "#ff3b6b",
  blue: "#3d6bff",
  ball: "#fff2a8",
  arrow: "#ffe66d",
  text: "#ffffff",
  subtitle: "#cfd6ff",
} as const;

const BIOME_PALETTE: Record<Biome, { fill: string; edge: string }> = {
  erba: { fill: "#1c8f5a", edge: "#27e0a0" },
  sabbia: { fill: "#c98a3a", edge: "#f2c66d" },
  neve: { fill: "#dfe9f2", edge: "#ffffff" },
  spiaggia: { fill: "#e8d9b5", edge: "#2fd7c4" },
};
```

Poi aggiorna `drawTerrain` per usare la tavolozza del bioma corrente. Sostituisci:

```ts
function drawTerrain(ctx: CanvasRenderingContext2D, s: GameState): void {
  const t = s.terrain;
  for (let x = 0; x < t.width; x += BLOCK) {
    const cx = Math.min(x + BLOCK / 2, t.width - 1);
    const top = Math.floor(heightAt(t, cx) / STEP) * STEP;
    ctx.fillStyle = COLORS.terrain;
    ctx.fillRect(x, top, BLOCK, s.height - top);
    ctx.fillStyle = COLORS.terrainEdge;
    ctx.fillRect(x, top, BLOCK, 4);
  }
}
```

con:

```ts
function drawTerrain(ctx: CanvasRenderingContext2D, s: GameState): void {
  const t = s.terrain;
  const palette = BIOME_PALETTE[s.biome];
  for (let x = 0; x < t.width; x += BLOCK) {
    const cx = Math.min(x + BLOCK / 2, t.width - 1);
    const top = Math.floor(heightAt(t, cx) / STEP) * STEP;
    ctx.fillStyle = palette.fill;
    ctx.fillRect(x, top, BLOCK, s.height - top);
    ctx.fillStyle = palette.edge;
    ctx.fillRect(x, top, BLOCK, 4);
  }
}
```

- [ ] **Step 4: Verifica lint e build**

Run: `npm run lint`
Atteso: nessun errore.

Run: `npm run build`
Atteso: "Compiled successfully".

- [ ] **Step 5: Playtest manuale**

Run: `npm run dev`, apri `http://localhost:3000/gioco`. Gioca fino a "Rigioca" (o ricarica la pagina) diverse volte di seguito:

- il colore del terreno cambia tra le partite (verde, tonalità calde/sabbia, bianco-azzurro/neve, sabbia chiara con bordo turchese/spiaggia) — nell'arco di ~8-10 partite dovresti vedere tutti e quattro;
- cielo, stelle, cannoni, palla e HUD **non** cambiano mai aspetto in base al bioma.

- [ ] **Step 6: Commit**

```bash
git add src/game/types.ts src/game/engine.ts src/game/render.ts
git commit -m "feat(gioco): aggiunge i biomi estetici del terreno"
```

---

## Task 3: Bersaglio power-up e modalità power

**Files:**
- Modify: `src/game/types.ts`
- Modify: `src/game/engine.ts`
- Modify: `src/game/render.ts`

**Interfaces:**
- Produce: tipi `PowerTarget` (`{ active: boolean; x: number; y: number; shotsLeft: number }`), `PowerTurns` (`Record<PlayerId, number>`); costanti `TARGET_RADIUS`, `POWER_SIZE_MULTIPLIER` (in `types.ts`); costanti `TARGET_HIT_RADIUS`, `TARGET_SPAWN_CHANCE`, `POWER_TURNS`, `POWER_DAMAGE_MULTIPLIER` (in `engine.ts`); campi `target: PowerTarget` e `power: PowerTurns` in `GameState`.
- Consuma: la struttura di `resolveBall`/`endShot` come modificata (solo nell'ordine dei controlli, non nella logica) dal Task 1; il campo `biome` del Task 2 non è toccato da questo task.

- [ ] **Step 1: Aggiungi i tipi e le costanti geometriche condivise**

In `src/game/types.ts`, aggiungi due nuove costanti condivise subito dopo `BALL_RADIUS`:

```ts
export const CANNON_RADIUS = 28; // raggio del corpo del cannone
export const BALL_RADIUS = 9; // raggio della palla
export const TARGET_RADIUS = 16; // raggio del bersaglio power-up nel cielo
export const POWER_SIZE_MULTIPLIER = 2; // ingrandimento (disegno e sagoma) di un cannone in modalità power
```

Poi aggiungi due nuove interfacce, subito dopo l'interfaccia `Wind`:

```ts
/** Bersaglio power-up nel cielo. */
export interface PowerTarget {
  active: boolean;
  x: number;
  y: number;
  shotsLeft: number; // tiri residui prima che scompaia se non colpito
}

/** Turni di modalità power residui per ciascun cannone (0 = non attiva). */
export type PowerTurns = Record<PlayerId, number>;
```

Infine aggiungi i due campi a `GameState` (dopo `biome: Biome;`):

```ts
  biome: Biome;
  target: PowerTarget;
  power: PowerTurns;
```

- [ ] **Step 2: Comparsa, scadenza e colpo del bersaglio; modalità power; danno e sagoma raddoppiati**

In `src/game/engine.ts`, aggiorna l'import dei tipi. Sostituisci:

```ts
import {
  type AimState,
  type Ball,
  type Biome,
  type GameState,
  type PlayerId,
  type Wind,
  BALL_RADIUS,
  CANNON_RADIUS,
} from "./types";
```

con:

```ts
import {
  type AimState,
  type Ball,
  type Biome,
  type GameState,
  type PlayerId,
  type PowerTarget,
  type Wind,
  BALL_RADIUS,
  CANNON_RADIUS,
  POWER_SIZE_MULTIPLIER,
  TARGET_RADIUS,
} from "./types";
```

Aggiungi le nuove costanti di regola, subito dopo `const MUZZLE = 60;`:

```ts
export const TARGET_HIT_RADIUS = TARGET_RADIUS + 6; // sagoma-bersaglio del power-up (più piccola di quella del cannone: bonus da abile)
export const TARGET_SPAWN_CHANCE = 0.22; // probabilità di comparsa a ogni turno (se non già attivo e nessuno è powered)
export const POWER_TURNS = 3; // durata della modalità power, in propri turni successivi
export const POWER_DAMAGE_MULTIPLIER = 2; // moltiplicatore del danno inflitto mentre si è in modalità power
```

Aggiungi una funzione per un bersaglio "vuoto" (nessun bersaglio attivo), subito dopo `function emptyBall(): Ball { ... }`:

```ts
function emptyTarget(): PowerTarget {
  return { active: false, x: 0, y: 0, shotsLeft: 0 };
}
```

Aggiungi la funzione che, a inizio turno, decide se far comparire un nuovo bersaglio, subito dopo `randomBiome`:

```ts
function maybeSpawnTarget(state: GameState): void {
  if (state.target.active) return; // c'è già un bersaglio in cielo
  if (state.power.red > 0 || state.power.blue > 0) return; // qualcuno è in modalità power
  if (Math.random() >= TARGET_SPAWN_CHANCE) return; // niente questo turno

  const marginX = state.width * 0.25;
  const x = marginX + Math.random() * (state.width - marginX * 2);
  const y = state.height * 0.2;
  state.target = { active: true, x, y, shotsLeft: 2 };
}
```

Nella funzione `createGame`, aggiungi `target` e `power` all'oggetto restituito (dopo `biome: randomBiome(rng),`):

```ts
    biome: randomBiome(rng),
    target: emptyTarget(),
    power: { red: 0, blue: 0 },
```

Nella funzione `startMatch`, fai comparire subito la possibilità di un bersaglio per il primo turno. Sostituisci:

```ts
/** Dalla schermata titolo (o per rigiocare) entra nella fase di mira. */
export function startMatch(state: GameState): void {
  state.phase = "aiming";
}
```

con:

```ts
/** Dalla schermata titolo (o per rigiocare) entra nella fase di mira. */
export function startMatch(state: GameState): void {
  state.phase = "aiming";
  maybeSpawnTarget(state);
}
```

Ora riscrivi `resolveBall` per controllare per primo il bersaglio, e per raddoppiare la sagoma del nemico quando è in modalità power. Sostituisci l'intera funzione:

```ts
function resolveBall(state: GameState): void {
  const b = state.ball;
  const enemyId = other(state.current);
  const enemy = state.cannons[enemyId];

  // Esce dai lati dello schermo → tiro a vuoto.
  if (b.x < 0 || b.x > state.width) {
    endShot(state, false, enemyId);
    return;
  }

  // Colpo diretto alla sagoma (generosa) del cannone nemico.
  if (Math.hypot(b.x - enemy.x, b.y - enemy.y) <= CANNON_HIT_RADIUS + BALL_RADIUS) {
    endShot(state, true, enemyId);
    return;
  }

  // Tocca il terreno → tiro a vuoto. (Il proprio cannone è ignorato: nessun autogol.)
  if (b.y + BALL_RADIUS >= heightAt(state.terrain, b.x)) {
    endShot(state, false, enemyId);
  }
}
```

con:

```ts
type ShotOutcome = "targetHit" | "enemyHit" | "miss";

function resolveBall(state: GameState): void {
  const b = state.ball;
  const enemyId = other(state.current);
  const enemy = state.cannons[enemyId];

  // 1. Bersaglio power-up (se attivo): ha priorità su tutto il resto.
  if (state.target.active) {
    const dTarget = Math.hypot(b.x - state.target.x, b.y - state.target.y);
    if (dTarget <= TARGET_HIT_RADIUS + BALL_RADIUS) {
      endShot(state, "targetHit", enemyId);
      return;
    }
  }

  // 2. Esce dai lati dello schermo → tiro a vuoto.
  if (b.x < 0 || b.x > state.width) {
    endShot(state, "miss", enemyId);
    return;
  }

  // 3. Colpo diretto alla sagoma del cannone nemico (raddoppiata se è in modalità power).
  const enemyHitRadius =
    state.power[enemyId] > 0 ? CANNON_HIT_RADIUS * POWER_SIZE_MULTIPLIER : CANNON_HIT_RADIUS;
  if (Math.hypot(b.x - enemy.x, b.y - enemy.y) <= enemyHitRadius + BALL_RADIUS) {
    endShot(state, "enemyHit", enemyId);
    return;
  }

  // 4. Tocca il terreno → tiro a vuoto. (Il proprio cannone è ignorato: nessun autogol.)
  if (b.y + BALL_RADIUS >= heightAt(state.terrain, b.x)) {
    endShot(state, "miss", enemyId);
  }
}
```

Infine riscrivi `endShot` per gestire il bersaglio, la modalità power (con il danno raddoppiato) e il vento/turno come prima. Sostituisci l'intera funzione:

```ts
function endShot(state: GameState, hit: boolean, enemyId: PlayerId): void {
  state.ball.active = false;

  if (hit) {
    const enemy = state.cannons[enemyId];
    enemy.health = Math.max(0, enemy.health - DAMAGE);
    if (enemy.health <= 0) {
      state.phase = "gameover";
      state.winner = state.current;
      return;
    }
  }

  // Vento: cambia ogni 2 tiri conclusi (un round completo di entrambi i giocatori).
  state.shotsSinceWindChange += 1;
  if (state.shotsSinceWindChange >= 2) {
    state.wind = randomWind(Math.random);
    state.shotsSinceWindChange = 0;
  }

  // Passa il turno.
  state.current = other(state.current);
  state.phase = "aiming";
}
```

con:

```ts
function endShot(state: GameState, outcome: ShotOutcome, enemyId: PlayerId): void {
  state.ball.active = false;
  const shooter = state.current;
  // "Ero in modalità power PRIMA di questo tiro?" — decide il moltiplicatore
  // del danno. Va calcolato prima di scalare il contatore qui sotto, altrimenti
  // il terzo (ultimo) turno powered perderebbe il raddoppio del danno.
  const wasPowered = state.power[shooter] > 0;

  if (outcome === "targetHit") {
    state.target = emptyTarget();
  } else if (outcome === "enemyHit") {
    const enemy = state.cannons[enemyId];
    const damage = wasPowered ? DAMAGE * POWER_DAMAGE_MULTIPLIER : DAMAGE;
    enemy.health = Math.max(0, enemy.health - damage);
    if (enemy.health <= 0) {
      state.phase = "gameover";
      state.winner = shooter;
      return;
    }
  }

  // La modalità power del tiratore scala di 1 per il tiro appena concluso
  // (a segno, a vuoto, o sul bersaglio: conta comunque come un turno).
  if (wasPowered) state.power[shooter] -= 1;

  // Il tiro che colpisce il bersaglio attiva la modalità power: vale dal
  // turno SUCCESSIVO di questo giocatore (questo tiro non è uno dei 3).
  if (outcome === "targetHit") state.power[shooter] = POWER_TURNS;

  // Vento: cambia ogni 2 tiri conclusi (un round completo di entrambi i giocatori).
  state.shotsSinceWindChange += 1;
  if (state.shotsSinceWindChange >= 2) {
    state.wind = randomWind(Math.random);
    state.shotsSinceWindChange = 0;
  }

  // Passa il turno.
  state.current = other(shooter);
  state.phase = "aiming";
  maybeSpawnTarget(state);
}
```

- [ ] **Step 3: Disegna il bersaglio, il cannone ingrandito con aura, e il badge dei turni di power**

In `src/game/render.ts`, aggiorna l'import aggiungendo le nuove costanti. Sostituisci:

```ts
import {
  type Biome,
  type GameState,
  type PlayerId,
  BALL_RADIUS,
  CANNON_RADIUS,
} from "./types";
```

con:

```ts
import {
  type Biome,
  type GameState,
  type PlayerId,
  BALL_RADIUS,
  CANNON_RADIUS,
  POWER_SIZE_MULTIPLIER,
  TARGET_RADIUS,
} from "./types";
```

Aggiungi la funzione che disegna il bersaglio nel cielo, subito prima di `function drawCannon`:

```ts
function drawTarget(ctx: CanvasRenderingContext2D, s: GameState): void {
  if (!s.target.active) return;
  const t = s.target;
  ctx.save();
  ctx.translate(t.x, t.y);
  ctx.shadowColor = COLORS.arrow;
  ctx.shadowBlur = 20;
  ctx.fillStyle = COLORS.arrow;
  ctx.beginPath();
  ctx.arc(0, 0, TARGET_RADIUS, 0, Math.PI * 2);
  ctx.fill();
  ctx.shadowBlur = 0;
  ctx.fillStyle = COLORS.skyTop;
  ctx.beginPath();
  ctx.arc(0, 0, TARGET_RADIUS / 2, 0, Math.PI * 2);
  ctx.fill();
  ctx.restore();
}
```

Chiama `drawTarget` dentro `draw`, subito dopo `drawTerrain(ctx, s);`. Sostituisci:

```ts
  drawTerrain(ctx, s);
  drawCannon(ctx, s, "red");
  drawCannon(ctx, s, "blue");
```

con:

```ts
  drawTerrain(ctx, s);
  drawTarget(ctx, s);
  drawCannon(ctx, s, "red");
  drawCannon(ctx, s, "blue");
```

Aggiorna `drawCannon` per ingrandire il cannone e disegnare l'aura quando è in modalità power. Sostituisci l'intera funzione:

```ts
function drawCannon(ctx: CanvasRenderingContext2D, s: GameState, id: PlayerId): void {
  const c = s.cannons[id];
  const color = playerColor(id);
  ctx.save();
  ctx.translate(c.x, c.y);
  ctx.shadowColor = color;
  ctx.shadowBlur = 24;

  // Canna (ruota con l'angolo di mira).
  ctx.save();
  ctx.rotate(c.angle);
  ctx.fillStyle = color;
  roundRect(ctx, -12, -14, 68, 28, 14);
  ctx.fill();
  ctx.restore();

  // Corpo del cannone.
  ctx.fillStyle = color;
  ctx.beginPath();
  ctx.arc(0, 0, CANNON_RADIUS, 0, Math.PI * 2);
  ctx.fill();

  // Foro centrale (senza bagliore).
  ctx.shadowBlur = 0;
  ctx.fillStyle = COLORS.skyTop;
  ctx.beginPath();
  ctx.arc(0, 0, CANNON_RADIUS / 2, 0, Math.PI * 2);
  ctx.fill();
  ctx.restore();
}
```

con:

```ts
function drawCannon(ctx: CanvasRenderingContext2D, s: GameState, id: PlayerId): void {
  const c = s.cannons[id];
  const color = playerColor(id);
  const powered = s.power[id] > 0;
  const scale = powered ? POWER_SIZE_MULTIPLIER : 1;
  ctx.save();
  ctx.translate(c.x, c.y);

  if (powered) {
    // Aura di modalità power: un alone più ampio e più luminoso attorno al cannone.
    ctx.save();
    ctx.shadowColor = color;
    ctx.shadowBlur = 40;
    ctx.globalAlpha = 0.35;
    ctx.fillStyle = color;
    ctx.beginPath();
    ctx.arc(0, 0, CANNON_RADIUS * scale * 1.35, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }

  ctx.shadowColor = color;
  ctx.shadowBlur = 24;

  // Canna (ruota con l'angolo di mira).
  ctx.save();
  ctx.rotate(c.angle);
  ctx.scale(scale, scale);
  ctx.fillStyle = color;
  roundRect(ctx, -12, -14, 68, 28, 14);
  ctx.fill();
  ctx.restore();

  // Corpo del cannone.
  ctx.fillStyle = color;
  ctx.beginPath();
  ctx.arc(0, 0, CANNON_RADIUS * scale, 0, Math.PI * 2);
  ctx.fill();

  // Foro centrale (senza bagliore).
  ctx.shadowBlur = 0;
  ctx.fillStyle = COLORS.skyTop;
  ctx.beginPath();
  ctx.arc(0, 0, (CANNON_RADIUS * scale) / 2, 0, Math.PI * 2);
  ctx.fill();
  ctx.restore();
}
```

Aggiorna `drawAim` così la percentuale di potenza resti sopra il cannone anche quando è ingrandito. Sostituisci:

```ts
function drawAim(ctx: CanvasRenderingContext2D, s: GameState): void {
  const a = s.aim;
  const c = s.cannons[s.current];
  const len = 40 + a.power * 90; // lunghezza della freccia in base alla potenza
```

con:

```ts
function drawAim(ctx: CanvasRenderingContext2D, s: GameState): void {
  const a = s.aim;
  const c = s.cannons[s.current];
  const scale = s.power[s.current] > 0 ? POWER_SIZE_MULTIPLIER : 1;
  const len = 40 + a.power * 90; // lunghezza della freccia in base alla potenza
```

e più sotto, nello stesso `drawAim`, sostituisci:

```ts
  ctx.fillText(`${Math.round(a.power * 100)}%`, c.x, c.y - CANNON_RADIUS - 16);
```

con:

```ts
  ctx.fillText(`${Math.round(a.power * 100)}%`, c.x, c.y - CANNON_RADIUS * scale - 16);
```

Infine, aggiungi il badge dei turni di power residui accanto alla percentuale di vita. Sostituisci in `drawHud`:

```ts
function drawHud(ctx: CanvasRenderingContext2D, s: GameState): void {
  drawHealth(ctx, 20, 20, s.cannons.red.health, COLORS.red, "ROSSO", "left");
  drawHealth(ctx, s.width - 20, 20, s.cannons.blue.health, COLORS.blue, "BLU", "right");
```

con:

```ts
function drawHud(ctx: CanvasRenderingContext2D, s: GameState): void {
  drawHealth(ctx, 20, 20, s.cannons.red.health, COLORS.red, "ROSSO", "left", s.power.red);
  drawHealth(
    ctx,
    s.width - 20,
    20,
    s.cannons.blue.health,
    COLORS.blue,
    "BLU",
    "right",
    s.power.blue,
  );
```

e aggiorna la firma e il testo di `drawHealth`. Sostituisci:

```ts
function drawHealth(
  ctx: CanvasRenderingContext2D,
  x: number,
  y: number,
  hp: number,
  color: string,
  label: string,
  align: "left" | "right",
): void {
```

con:

```ts
function drawHealth(
  ctx: CanvasRenderingContext2D,
  x: number,
  y: number,
  hp: number,
  color: string,
  label: string,
  align: "left" | "right",
  powerTurns: number,
): void {
```

e sostituisci la riga del testo:

```ts
  ctx.fillText(`${label} ${hp}%`, align === "left" ? bx : bx + w, y + h + 14);
```

con:

```ts
  const suffix = powerTurns > 0 ? ` ⚡×${powerTurns}` : "";
  ctx.fillText(`${label} ${hp}%${suffix}`, align === "left" ? bx : bx + w, y + h + 14);
```

- [ ] **Step 4: Verifica lint e build**

Run: `npm run lint`
Atteso: nessun errore.

Run: `npm run build`
Atteso: "Compiled successfully".

- [ ] **Step 5: Playtest manuale**

Run: `npm run dev`, apri `http://localhost:3000/gioco`. Gioca finché non compare un bersaglio dorato nel cielo (se serve, ricarica/rigioca alcune volte: la probabilità è ~20-25% a turno):

- il bersaglio resta visibile per i due tiri successivi alla comparsa (quello di chi era di turno e quello dell'altro giocatore), poi sparisce da solo se nessuno lo colpisce;
- mirandolo e colpendolo con il tiro normale, il **proprio** cannone raddoppia di dimensione con un'aura, e vicino alla sua barra vita compare "⚡×3";
- nei 3 turni successivi di quel giocatore, un colpo a segno toglie il **40%** di vita all'avversario (non il 20%) e il badge scala a "⚡×2", poi "⚡×1", poi sparisce; al quarto turno successivo il danno torna al 20% normale;
- mentre un cannone è ingrandito, è anche **più facile da colpire** per l'avversario (la sagoma raddoppiata);
- essere colpiti mentre si è in modalità power toglie comunque il 20% normale (il raddoppio vale solo per il danno inflitto);
- mentre un cannone è in modalità power, **nessun nuovo bersaglio** compare, nemmeno dopo diversi turni.

- [ ] **Step 6: Commit**

```bash
git add src/game/types.ts src/game/engine.ts src/game/render.ts
git commit -m "feat(gioco): aggiunge il bersaglio power-up e la modalità power"
```

---

## Task 4: Playtest finale e Definition of Done

**Files:** nessuna modifica prevista (solo verifica; correggi e ricommetti se emergono problemi).

- [ ] **Step 1: Build e lint puliti**

Run: `npm run build`
Atteso: "Compiled successfully", rotte `/` e `/gioco` presenti.

Run: `npm run lint`
Atteso: nessun errore.

- [ ] **Step 2: Partita completa con tutte e tre le funzionalità**

Con `npm run dev`, gioca una o più partite complete fino alla vittoria, verificando insieme (non solo singolarmente come nei task precedenti):

- il vento è sempre visibile e cambia per round, indipendentemente dal bioma o dalla presenza di un bersaglio;
- il bersaglio power-up compare/scompare correttamente e la modalità power funziona (dimensione, aura, danno doppio, sagoma doppia) anche con il vento attivo (il tiro verso il bersaglio risente comunque del vento);
- il bioma scelto per la partita resta coerente per tutta la sua durata e non interferisce con nessuna delle altre due funzionalità;
- le regole di base della v1 restano intatte: vita 100%→0%, colpo diretto, terreno fisso e simmetrico, nessun autogol, passaggio del turno, vittoria e "Rigioca".

- [ ] **Step 3: (Se necessario) regola il bilanciamento e ricommetti**

Se il bersaglio power-up compare troppo spesso o troppo raramente, regola `TARGET_SPAWN_CHANCE` in `src/game/engine.ts`. Se il vento risulta troppo debole o troppo forte, regola `WIND_MAX_ACCEL` in `src/game/physics.ts`. Poi:

```bash
git add -A
git commit -m "tune(gioco): bilanciamento vento/power-up dopo il playtest v1.1"
```

---

## Self-Review (compilata dall'autore del piano)

**1. Copertura della spec:**
- Vento: generazione casuale, fissa per round, effetto fisico leggero-moderato, indicatore sempre visibile → Task 1. ✓
- Bersaglio power-up: comparsa probabilistica, niente spawn se qualcuno è powered, durata 2 tiri, colpito col tiro normale, ordine dei controlli → Task 3 (`maybeSpawnTarget`, `resolveBall`). ✓
- Modalità power: 3 turni propri successivi (il tiro che attiva non conta), cannone e sagoma raddoppiati, danno inflitto raddoppiato, danno subito invariato → Task 3 (`endShot`, `drawCannon`, `resolveBall`). ✓
- Biomi: uno scelto a caso a inizio partita, solo tavolozza del terreno, resto della scena invariato → Task 2. ✓
- Definition of Done (build, lint, playtest) → Task 4. ✓

**2. Scansione segnaposto:** nessun "TBD/TODO/da completare"; ogni step che tocca codice mostra il codice completo (vecchio e nuovo, dove si sostituisce).

**3. Coerenza dei tipi e dei nomi:** `Wind`/`windAccelFor`/`stepBall(ball, dt, windAccelX)` (Task 1) sono usati con lo stesso nome in Task 1 Step 3 e in `GameCanvas.tsx` (invariato, chiama `update(s, STEP)` che internamente ora passa il vento — nessuna modifica a `GameCanvas.tsx` necessaria). `Biome`/`BIOME_PALETTE`/`s.biome` (Task 2) coerenti tra `engine.ts` e `render.ts`. `PowerTarget`/`PowerTurns`/`TARGET_RADIUS`/`POWER_SIZE_MULTIPLIER`/`TARGET_HIT_RADIUS`/`TARGET_SPAWN_CHANCE`/`POWER_TURNS`/`POWER_DAMAGE_MULTIPLIER` (Task 3) usati con lo stesso nome tra `types.ts`, `engine.ts` e `render.ts`. La firma di `endShot` cambia da `(state, hit: boolean, enemyId)` a `(state, outcome: ShotOutcome, enemyId)` interamente dentro il Task 3 (nessun altro file chiama `endShot` direttamente, è una funzione privata del modulo). La firma di `drawHealth` cambia aggiungendo `powerTurns: number` in Task 3, e i due punti di chiamata in `drawHud` vengono aggiornati nello stesso step.
